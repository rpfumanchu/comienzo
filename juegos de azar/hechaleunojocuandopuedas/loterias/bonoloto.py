from utils.number_generator import number_generator

def num_bonoloto():
    number_generator(6, 1, 49, "Tus números son: ")

# Te lo escribo en todas las funciones donde estaba.

# Esto no lo hagas porque si lo haces, entonces nunca voy a poder usar esta libreria sin ejecutar nada,
#  y el objetivo es exportar funcionalidades, no ejecutarlas para ejecutarlas, se encarga el que haga el import de esta libreria
# num_bonoloto()